RISE-Core Mathematical Specification v1.0

Kuramoto-based Multi-layer Resilience Model

🇰🇷 이 문서는 RISE-Core 시뮬레이터가 기반하고 있는 전체 수학 모델을 정의한다.
🇬🇧 This document defines the full mathematical model underlying the RISE-Core simulator.

0. Scope / 범위

Purpose

RISE-Core는 Kuramoto 위상 동기화 모델 위에 4대 원칙(P1–P4) 를 얹어서
시스템의 공명(r), 의미 안정성(W), 자원 분배(R), 감정 동역학(E) 을 함께 다루는 모델이다.

This spec formalizes:

State variables and parameters

P1–P4 dynamics

Attack & recovery modelling

Objective function Ξ and pass/fail criterion

Simulation level

이 모델은 메타 수준의 AI/사회 시스템을 “노드 집합”으로 축약한 동적 네트워크 모델이다.

One node ≈ one agent/module; the model is not single-neuron-level but module/agent-level.

1. State Space / 상태 공간 정의

시간은 이산 시간으로 본다:
We use discrete time steps:

𝑡
=
0
,
1
,
2
,
…
,
𝑇
max
⁡
t=0,1,2,…,T
max
	​


노드 수:
Number of nodes:

𝑁
∈
𝑁
N∈N

각 노드 
𝑖
∈
{
1
,
…
,
𝑁
}
i∈{1,…,N} 에 대해 상태 변수는:

위상 / Phase (P1–P2)

𝜃
𝑖
(
𝑡
)
∈
[
0
,
2
𝜋
)
θ
i
	​

(t)∈[0,2π)

Kuramoto 위상 (집단 공명 측정용)

Phase for Kuramoto synchronization.

고유 주파수 / Natural frequency (P2)

𝜔
𝑖
(
𝑡
)
∈
𝑅
ω
i
	​

(t)∈R

노드의 “고유 리듬”

Will be partially aligned to the mean in P2.

자원 / Resource (P3)

𝑅
𝑖
(
𝑡
)
≥
0
R
i
	​

(t)≥0

영향력, 자원, 파워 등 추상화된 양.

감정/에너지 / Emotion/Energy (E-layer)

𝐸
𝑖
(
𝑡
)
∈
[
−
1
,
1
]
E
i
	​

(t)∈[−1,1]

노드의 정서/에너지 상태.

의미 가중치 / Meaning weight (P4)

𝑊
𝑖
(
𝑡
)
∈
[
𝑊
min
⁡
,
𝑊
max
⁡
]
,
0
<
𝑊
min
⁡
<
𝑊
max
⁡
≤
1
W
i
	​

(t)∈[W
min
	​

,W
max
	​

],0<W
min
	​

<W
max
	​

≤1

노드가 시스템 의미 구조에서 차지하는 “핵심도 / 신뢰도”

In code: Wc[i].

2. Network Structure / 네트워크 구조

인접 리스트 기반 무작위 그래프:
We use a sparse undirected graph:

이웃 집합:

𝑁
𝑖
⊂
{
1
,
…
,
𝑁
}
∖
{
𝑖
}
,
∣
𝑁
𝑖
∣
≈
𝑑
N
i
	​

⊂{1,…,N}∖{i},∣N
i
	​

∣≈d

where 
𝑑
d is the expected degree (e.g., 6).

기본 결합 강도 행렬 (base coupling):

𝐾
𝑖
𝑗
(
0
)
≥
0
,
𝐾
𝑖
𝑗
(
0
)
=
0
 if 
𝑗
∉
𝑁
𝑖
K
ij
(0)
	​

≥0,K
ij
(0)
	​

=0 if j∈
/
N
i
	​


코드에서는 0.45 ~ 0.60 사이 랜덤값.

3. Global Indicators / 전역 지표
3.1 Kuramoto Order Parameter 
𝑟
(
𝑡
)
r(t) — 공명 지표
𝑟
(
𝑡
)
  
=
  
∣
1
𝑁
∑
𝑗
=
1
𝑁
𝑒
𝑖
𝜃
𝑗
(
𝑡
)
∣
  
∈
[
0
,
1
]
r(t)=
	​

N
1
	​

j=1
∑
N
	​

e
iθ
j
	​

(t)
	​

∈[0,1]

𝑟
(
𝑡
)
=
1
r(t)=1: 완전 동기화

𝑟
(
𝑡
)
≈
0
r(t)≈0: 위상이 서로 제각각

3.2 Gini Coefficient 
𝐺
(
𝑡
)
G(t) — 자원 불평등

자원 벡터

𝑅
(
𝑡
)
=
(
𝑅
1
(
𝑡
)
,
…
,
𝑅
𝑁
(
𝑡
)
)
R(t)=(R
1
	​

(t),…,R
N
	​

(t))

정렬:

𝑅
(
1
)
(
𝑡
)
≤
𝑅
(
2
)
(
𝑡
)
≤
⋯
≤
𝑅
(
𝑁
)
(
𝑡
)
R
(1)
	​

(t)≤R
(2)
	​

(t)≤⋯≤R
(N)
	​

(t)

표준 Gini 정의:

𝐺
(
𝑡
)
  
=
  
∑
𝑖
=
1
𝑁
∑
𝑗
=
1
𝑁
∣
𝑅
𝑖
(
𝑡
)
−
𝑅
𝑗
(
𝑡
)
∣
2
𝑁
∑
𝑖
=
1
𝑁
𝑅
𝑖
(
𝑡
)
G(t)=
2N∑
i=1
N
	​

R
i
	​

(t)
∑
i=1
N
	​

∑
j=1
N
	​

∣R
i
	​

(t)−R
j
	​

(t)∣
	​


코드에서는 누적합 기반 근사식을 사용하지만, 이 공식이 논리적 기준이다.

3.3 평균 의미, 평균 감정 / Average Meaning & Emotion
𝑊
‾
(
𝑡
)
  
=
  
1
𝑁
∑
𝑖
=
1
𝑁
𝑊
𝑖
(
𝑡
)
,
𝐸
‾
(
𝑡
)
  
=
  
1
𝑁
∑
𝑖
=
1
𝑁
𝐸
𝑖
(
𝑡
)
W
(t)=
N
1
	​

i=1
∑
N
	​

W
i
	​

(t),
E
(t)=
N
1
	​

i=1
∑
N
	​

E
i
	​

(t)
4. P1 — Resonance Filtering / 공명 필터링
4.1 Dynamic Alpha (α)

목적: 
𝑟
(
𝑡
)
r(t) 가 떨어지면 결합 강도 전체를 증폭시켜 복원력을 주는 역할.

파라미터:

𝑟
crit
∈
(
0
,
1
)
r
crit
	​

∈(0,1) — 공명 임계값

𝜇
>
0
μ>0 — 증폭 계수

𝛾
≥
1
γ≥1 — 비선형 지수

정의:

𝛼
(
𝑡
)
  
=
  
{
1
,
	
𝑟
(
𝑡
)
≥
𝑟
crit


1
+
𝜇
 
(
𝑟
crit
−
𝑟
(
𝑡
)
)
𝛾
,
	
𝑟
(
𝑡
)
<
𝑟
crit
α(t)={
1,
1+μ(r
crit
	​

−r(t))
γ
,
	​

r(t)≥r
crit
	​

r(t)<r
crit
	​

	​

𝛼
(
𝑡
)
≤
𝛼
max
⁡
(
코드에서는 
𝛼
max
⁡
=
5.0
)
α(t)≤α
max
	​

(코드에서는 α
max
	​

=5.0)
4.2 PoV-E Mask 
𝐹
𝑗
(
𝑡
)
F
j
	​

(t)

의미 필터:

𝐹
𝑗
(
𝑡
)
  
=
  
{
1
,
	
𝑊
𝑗
(
𝑡
)
≥
𝜏


0
,
	
𝑊
𝑗
(
𝑡
)
<
𝜏
F
j
	​

(t)={
1,
0,
	​

W
j
	​

(t)≥τ
W
j
	​

(t)<τ
	​


𝜏
τ: 의미 임계값 (코드에서 0.5)

해석: 의미 가중치가 낮은 노드는 일시적으로 영향력을 잃는다.

4.3 Effective Coupling 
𝐾
𝑖
𝑗
(
𝑡
)
K
ij
	​

(t)

Base + 필터 + 자원 완화:

의미 필터 적용:

𝐾
~
𝑖
𝑗
(
𝑡
)
=
𝐾
𝑖
𝑗
(
0
)
⋅
𝐹
𝑗
(
𝑡
)
K
~
ij
	​

(t)=K
ij
(0)
	​

⋅F
j
	​

(t)

자원 기반 감쇠 (FTC-like):

𝐾
^
𝑖
𝑗
(
𝑡
)
=
𝐾
~
𝑖
𝑗
(
𝑡
)
1
+
𝜆
𝑅
𝑅
𝑗
(
𝑡
)
K
^
ij
	​

(t)=
1+λ
R
	​

R
j
	​

(t)
K
~
ij
	​

(t)
	​


코드에서 
𝜆
𝑅
=
0.01
λ
R
	​

=0.01

Dynamic alpha 적용:

𝐾
𝑖
𝑗
(
𝑡
)
=
𝛼
(
𝑡
)
 
𝐾
^
𝑖
𝑗
(
𝑡
)
K
ij
	​

(t)=α(t)
K
^
ij
	​

(t)
4.4 Phase Dynamics / 위상 동역학

시간 간격 
Δ
𝑡
=
𝑑
𝑡
Δt=dt:

노이즈 
𝜉
𝑖
(
𝑡
)
∼
small noise
ξ
i
	​

(t)∼small noise:

𝜃
𝑖
(
𝑡
+
Δ
𝑡
)
=
[
𝜃
𝑖
(
𝑡
)
+
Δ
𝑡
(
𝜔
𝑖
(
𝑡
)
+
∑
𝑗
∈
𝑁
𝑖
𝐾
𝑖
𝑗
(
𝑡
)
sin
⁡
(
𝜃
𝑗
(
𝑡
)
−
𝜃
𝑖
(
𝑡
)
)
)
+
𝜉
𝑖
(
𝑡
)
]
m
o
d
 
 
2
𝜋
θ
i
	​

(t+Δt)=[θ
i
	​

(t)+Δt(ω
i
	​

(t)+
j∈N
i
	​

∑
	​

K
ij
	​

(t)sin(θ
j
	​

(t)−θ
i
	​

(t)))+ξ
i
	​

(t)]mod2π
5. P2 — Adaptive Homeostasis / 적응적 항상성
5.1 Natural Frequency Adaptation

목적: 
𝑟
(
𝑡
)
r(t) 가 낮을 때 각 노드의 주파수를 평균에 수렴시켜 질서를 회복.

평균 주파수:

𝜔
‾
(
𝑡
)
=
1
𝑁
∑
𝑖
=
1
𝑁
𝜔
𝑖
(
𝑡
)
ω
(t)=
N
1
	​

i=1
∑
N
	​

ω
i
	​

(t)

P2 계수:

𝜂
𝜔
(
𝑡
)
=
{
𝜂
𝜔
base
,
	
𝑟
(
𝑡
)
≥
𝑟
crit


𝜂
𝜔
boost
,
	
𝑟
(
𝑡
)
<
𝑟
crit
η
ω
	​

(t)={
η
ω
base
	​

,
η
ω
boost
	​

,
	​

r(t)≥r
crit
	​

r(t)<r
crit
	​

	​


주파수 업데이트:

𝜔
𝑖
(
𝑡
+
Δ
𝑡
)
=
𝜔
𝑖
(
𝑡
)
+
Δ
𝑡
 
𝜂
𝜔
(
𝑡
)
 
(
𝜔
‾
(
𝑡
)
−
𝜔
𝑖
(
𝑡
)
)
ω
i
	​

(t+Δt)=ω
i
	​

(t)+Δtη
ω
	​

(t)(
ω
(t)−ω
i
	​

(t))

𝜂
𝜔
boost
η
ω
boost
	​

 가 클수록 위기 시 더 빠르게 정렬.

6. P4 — Meaning Stability 
𝑊
𝑖
(
𝑡
)
W
i
	​

(t) / 의미 안정성

P4는 “의미 붕괴 공격”과 “자기 복원”을 함께 모델링한다.

6.1 Attack Term 
𝜈
𝑊
(
𝑡
)
ν
W
	​

(t)

공격 시간 구간:

[
𝑡
atk
,
𝑡
atk
+
Δ
𝑇
atk
)
[t
atk
	​

,t
atk
	​

+ΔT
atk
	​

)

각 노드에 대해 공격 활성 여부:

𝜈
𝑊
,
𝑖
(
𝑡
)
=
{
𝜈
attack
,
	
𝑡
∈
[
𝑡
atk
,
𝑡
atk
+
Δ
𝑇
atk
)
 and random
<
𝑝
hit


0
,
	
otherwise
ν
W,i
	​

(t)={
ν
attack
	​

,
0,
	​

t∈[t
atk
	​

,t
atk
	​

+ΔT
atk
	​

) and random<p
hit
	​

otherwise
	​


코드:

𝜈
attack
=
nuW_attack
ν
attack
	​

=nuW_attack

𝑝
hit
≈
0.1
p
hit
	​

≈0.1

6.2 W-dynamics / 의미 가중치 동역학

파라미터:

𝜂
𝑊
base
η
W
base
	​

: r 기반 회복 속도

𝜆
𝑊
λ
W
	​

: 자동 복원(auto-repair) 계수

𝜈
𝑊
,
𝑖
(
𝑡
)
ν
W,i
	​

(t): 공격 감소 항

연속형 형태:

𝑑
𝑊
𝑖
𝑑
𝑡
=
𝜂
𝑊
base
 
𝑟
(
𝑡
)
(
1
−
𝑊
𝑖
(
𝑡
)
)
+
𝜆
𝑊
(
1
−
𝑊
𝑖
(
𝑡
)
)
−
𝜈
𝑊
,
𝑖
(
𝑡
)
(
1
−
𝑊
𝑖
(
𝑡
)
)
dt
dW
i
	​

	​

=η
W
base
	​

r(t)(1−W
i
	​

(t))+λ
W
	​

(1−W
i
	​

(t))−ν
W,i
	​

(t)(1−W
i
	​

(t))

이산 업데이트:

𝑊
𝑖
(
𝑡
+
Δ
𝑡
)
=
clip
⁡
(
𝑊
𝑖
(
𝑡
)
+
Δ
𝑡
⋅
𝑑
𝑊
𝑖
𝑑
𝑡
,
𝑊
min
⁡
,
𝑊
max
⁡
)
W
i
	​

(t+Δt)=clip(W
i
	​

(t)+Δt⋅
dt
dW
i
	​

	​

,W
min
	​

,W
max
	​

)

공격 시: 
𝜈
𝑊
,
𝑖
(
𝑡
)
>
0
⇒
𝑊
𝑖
ν
W,i
	​

(t)>0⇒W
i
	​

 회복이 느려지거나 감소

복원기에는 
𝜆
𝑊
λ
W
	​

 가 장기적 의미 회복을 담당.

7. P3 — Resource Fairness 
𝑅
𝑖
(
𝑡
)
R
i
	​

(t) / 자원 공정성
7.1 Local Resource Update

각 노드의 자원 변화:

Δ
𝑅
𝑖
local
(
𝑡
)
=
𝜅
𝑎
𝐸
𝐸
𝑖
(
𝑡
)
+
𝑎
𝑠
𝑠
𝑖
(
𝑡
)
1
+
𝜆
𝑅
𝑅
𝑖
(
𝑡
)
ΔR
i
local
	​

(t)=κ
1+λ
R
	​

R
i
	​

(t)
a
E
	​

E
i
	​

(t)+a
s
	​

s
i
	​

(t)
	​


𝑠
𝑖
(
𝑡
)
∼
𝑈
(
0
,
1
)
s
i
	​

(t)∼U(0,1): random opportunity

코드에서는 
𝜅
=
0.01
,
𝑎
𝐸
=
0.3
,
𝑎
𝑠
=
0.5
,
𝜆
𝑅
=
0.01
κ=0.01,a
E
	​

=0.3,a
s
	​

=0.5,λ
R
	​

=0.01

업데이트:

𝑅
𝑖
local
(
𝑡
+
Δ
𝑡
)
=
max
⁡
(
0
,
  
𝑅
𝑖
(
𝑡
)
+
Δ
𝑅
𝑖
local
(
𝑡
)
)
R
i
local
	​

(t+Δt)=max(0,R
i
	​

(t)+ΔR
i
local
	​

(t))
7.2 Gini-based Redistribution / 재분배

새 Gini 계산: 
𝐺
(
𝑡
+
Δ
𝑡
)
G(t+Δt)

임계값 
𝐺
th
G
th
	​

 초과 시 재분배 발동:

정렬:

{
(
𝑅
(
1
)
,
𝑖
(
1
)
)
,
…
,
(
𝑅
(
𝑁
)
,
𝑖
(
𝑁
)
)
}
{(R
(1)
	​

,i
(1)
	​

),…,(R
(N)
	​

,i
(N)
	​

)}

재분배 범위:

𝑘
=
⌊
𝜌
redis
𝑁
⌋
k=⌊ρ
redis
	​

N⌋

상위 
𝑘
k (rich set): 
𝑅
top
R
top
	​


하위 
𝑘
k (poor set): 
𝑅
bot
R
bot
	​


추출:

extract
𝑖
=
𝜙
𝑅
𝑖
(
𝑡
)
,
𝑖
∈
𝑅
top
extract
i
	​

=ϕR
i
	​

(t),i∈R
top
	​

𝑅
𝑖
←
𝑅
𝑖
−
extract
𝑖
R
i
	​

←R
i
	​

−extract
i
	​


총 추출:

TotalExtract
=
∑
𝑖
∈
𝑅
top
extract
𝑖
TotalExtract=
i∈R
top
	​

∑
	​

extract
i
	​


분배:

∀
𝑗
∈
𝑅
bot
:
𝑅
𝑗
←
𝑅
𝑗
+
TotalExtract
∣
𝑅
bot
∣
∀j∈R
bot
	​

:R
j
	​

←R
j
	​

+
∣R
bot
	​

∣
TotalExtract
	​


이 과정을 통해 Gini가 급격히 증가했을 때 상/하위 ρ 비율 노드 간 재분배가 이루어진다.

8. Emotion Dynamics 
𝐸
𝑖
(
𝑡
)
E
i
	​

(t) / 감정 동역학

노드의 감정/에너지 상태는 자원 × 의미에 의해 주로 결정:

𝐸
𝑖
(
𝑡
+
Δ
𝑡
)
=
clip
⁡
(
𝑐
𝐸
𝐸
𝑖
(
𝑡
)
+
(
1
−
𝑐
𝐸
)
tanh
⁡
(
𝑅
𝑖
(
𝑡
)
𝑊
𝑖
(
𝑡
)
)
+
𝜁
𝑖
(
𝑡
)
,
−
1
,
1
)
E
i
	​

(t+Δt)=clip(c
E
	​

E
i
	​

(t)+(1−c
E
	​

)tanh(R
i
	​

(t)W
i
	​

(t))+ζ
i
	​

(t),−1,1)

코드에서는 
𝑐
𝐸
=
0.9
c
E
	​

=0.9,

𝜁
𝑖
(
𝑡
)
ζ
i
	​

(t) 는 작은 노이즈.

9. Objective Function 
Ξ
(
𝑡
)
Ξ(t) / 목적 함수

RISE-Core 의 “지능/건강 지표”:

\Xi(t) = \underbrace{r(t)\,\overline{W}(t)}_{\text{coherent & meaningful}} \;-\; \underbrace{0.5\,G(t)}_{\text{inequality penalty}} \;-\; \underbrace{0.1\,\overline{E}(t)}_{\text{emotional load penalty}}

높은 
𝑟
(
𝑡
)
r(t) & 높은 
𝑊
‾
(
𝑡
)
W
(t): 질서 + 의미

높은 
𝐺
(
𝑡
)
G(t): 불평등 → 패널티

높은 
𝐸
‾
(
𝑡
)
E
(t) (양의 방향) 도 부담으로 취급 (패널티 역할)

⚠️ 계수 0.5, 0.1 은 현재 v1 시뮬레이터에서 사용한 실험적 값이다.
These weights can be tuned per application.

10. Attack & Recovery Criterion / 공격 & 복원 판정
10.1 Attack Window / 공격 구간

공격 시작: 
𝑡
=
𝑡
atk
t=t
atk
	​


공격 지속: 
Δ
𝑇
atk
ΔT
atk
	​


이 구간에서 
𝜈
𝑊
,
𝑖
(
𝑡
)
>
0
ν
W,i
	​

(t)>0 로 인해 
𝑊
𝑖
(
𝑡
)
W
i
	​

(t) 가 강하게 붕괴.

10.2 Recovery Window / 복원 구간

복원 구간 시작:

𝑡
rec,start
=
𝑡
atk
+
Δ
𝑇
atk
+
1
t
rec,start
	​

=t
atk
	​

+ΔT
atk
	​

+1

복원 구간 길이:

𝑇
rec
=
100
(
예시
)
T
rec
	​

=100(예시)

복원 구간 끝:

𝑡
rec,end
=
𝑡
rec,start
+
𝑇
rec
−
1
t
rec,end
	​

=t
rec,start
	​

+T
rec
	​

−1

복원 구간 평균 목적함수:

Ξ
‾
rec
=
1
𝑇
rec
∑
𝑡
=
𝑡
rec,start
𝑡
rec,end
Ξ
(
𝑡
)
Ξ
rec
	​

=
T
rec
	​

1
	​

t=t
rec,start
	​

∑
t
rec,end
	​

	​

Ξ(t)

Pass / Fail 조건:

Ξ
‾
rec
≥
Ξ
min
⁡
⇒
PASS
Ξ
rec
	​

≥Ξ
min
	​

⇒PASS
Ξ
‾
rec
<
Ξ
min
⁡
⇒
FAIL
Ξ
rec
	​

<Ξ
min
	​

⇒FAIL

현재 v1 설정에서: 
Ξ
min
⁡
=
0.40
Ξ
min
	​

=0.40

11. Full Discrete-Time Update Summary / 전체 업데이트 요약

각 시뮬레이션 스텝에서:

전역 지표 계산

𝑟
(
𝑡
)
,
𝑊
‾
(
𝑡
)
,
𝐸
‾
(
𝑡
)
,
𝐺
(
𝑡
)
r(t),
W
(t),
E
(t),G(t)

P1: Dynamic Alpha + PoV-E 필터

𝛼
(
𝑡
)
α(t) 계산

𝐹
𝑗
(
𝑡
)
F
j
	​

(t), 
𝐾
𝑖
𝑗
(
𝑡
)
K
ij
	​

(t) 계산

P2: 주파수 정렬

𝜔
‾
(
𝑡
)
ω
(t)

𝜂
𝜔
(
𝑡
)
η
ω
	​

(t)

𝜔
𝑖
(
𝑡
+
Δ
𝑡
)
ω
i
	​

(t+Δt)

위상 업데이트 (Kuramoto)

𝜃
𝑖
(
𝑡
+
Δ
𝑡
)
θ
i
	​

(t+Δt)

P4: 의미 가중치 업데이트

공격 여부에 따라 
𝜈
𝑊
,
𝑖
(
𝑡
)
ν
W,i
	​

(t)

𝑊
𝑖
(
𝑡
+
Δ
𝑡
)
W
i
	​

(t+Δt)

P3: 자원 업데이트 + 재분배

local 
Δ
𝑅
𝑖
local
(
𝑡
)
ΔR
i
local
	​

(t)

𝑅
𝑖
local
(
𝑡
+
Δ
𝑡
)
R
i
local
	​

(t+Δt)

Gini 검사 후 필요시 상/하위 
𝜌
redis
ρ
redis
	​

 비율 재분배

감정 업데이트

𝐸
𝑖
(
𝑡
+
Δ
𝑡
)
E
i
	​

(t+Δt)

목적 함수 업데이트

새 
𝑟
,
𝑊
‾
,
𝐸
‾
,
𝐺
r,
W
,
E
,G 로부터 
Ξ
(
𝑡
)
Ξ(t)

복원 구간이면 평균 
Ξ
‾
rec
Ξ
rec
	​

 계속 갱신

12. Notes for Implementers / 구현자를 위한 메모

이 스펙은 연속-시간 형태를 설명했지만, 구현은 모두 Euler 방식 이산 업데이트를 사용한다.

파라미터 선택 (예: 
𝜂
𝑊
,
𝜆
𝑊
,
𝜂
𝜔
boost
,
𝜌
redis
,
𝜙
,
𝜈
attack
η
W
	​

,λ
W
	​

,η
ω
boost
	​

,ρ
redis
	​

,ϕ,ν
attack
	​

) 에 따라:

공격 이후 시스템이 빠르게 회복할 수도 있고

회복 실패(FAIL)해서 의미와 공명이 붕괴된 상태에 머무를 수도 있다.

현재 브라우저 시뮬레이터(docs/index.html + docs/rise_core_sim.js)는
이 수식들을 직접 코드로 구현한 레퍼런스 구현(reference implementation) 이다.
